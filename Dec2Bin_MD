#include <stdio.h>
int main()
{
char a = 2, x, b1, b2, b3, b4, b5, b6, b7, b8, b9, b10;

printf("Dec: ");
scanf("%d", &x);

	b1 = x % a;
		x = x >> 1;
	b2 = x % a;
		x = x >> 1;
	b3 = x % a;
		x = x >> 1;
	b4 = x % a;
		x = x >> 1;
	b5 = x % a;
		x = x >> 1;
	b6 = x % a;
		x = x >> 1;	
	b7= x % a;
		x = x >> 1;
	b8= x % a;
		x = x >> 1;
	b9= x % a;
		x = x >> 1;
	b10= x % a;
		x = x >> 1;

printf("Bin = %d%d%d%d%d%d%d%d%d%d\n”,b10, b9, b8, b7, b6, b5, b4, b3, b2, b1);

return 0;
}
